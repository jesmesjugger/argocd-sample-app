apiVersion: v1
kind: Service
metadata:
  name: argo-node-web-app-service
  namespace: argo-node-web-project
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: argo-node-web-app